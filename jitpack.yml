jdk:
  - openjdk17

install:
  - ./gradlew clean assembleRelease publishReleasePublicationToMavenRepository

build:
  artifacts:
    - app/build/outputs/aar/androidutils-release.aar
    - app/build/outputs/mapping/release/mapping.txt
    - app/build/libs/